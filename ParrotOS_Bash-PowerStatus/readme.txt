unzip hack in root's .local/share/fonts
if font is not present make a new folder called font

the update fonts in system 
# fc-cache -f -v 

then paste the starship.toml in your root's .config folder
